[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: όλες οι διαδρομές → index.html (Vite/React)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
